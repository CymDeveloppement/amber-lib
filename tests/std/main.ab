import * from "../test.ab"
import * from "../../src/std/main.ab"

fun replace_once_test() {
    return replace_once(\"hello world!\", \"world\", \"Amber\")
}

test_amber(replace_once_test(), "hello Amber!")